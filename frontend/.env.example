# URL de base du backend (ex. https://scorpanion.hexadj.com/api ou http://localhost:8080).
# Vous pouvez utiliser API_BASE_URL ou VITE_API_BASE_URL (les deux sont pris en charge).
# Si vide, le client utilise /api (proxy Vite vers localhost:8080 en dev).
API_BASE_URL=